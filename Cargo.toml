[package]
name        = "pt"
version     = "0.2.0"
edition     = "2021"
description = "Prayer times calculator — Egyptian General Authority of Survey"
authors     = ["Muhammad Samir"]

[[bin]]
name = "pt"
path = "src/main.rs"

[dependencies]
# Zero external dependencies — pure std Rust
